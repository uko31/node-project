list of users
<h3>Search Users</h3>
<form action='/users' method='GET'>
    <label>Name</label>
    <input type='text' name='username' id='username' value='<%= searchOptions.username %>'/>
    <button type='submit'>Search</button>
</form>
<br>
 <% users.forEach(user => { %>
    <div>
        <%=  user.username %>
    </div>
 <% }) %>